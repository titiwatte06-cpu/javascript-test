const prompt = require('prompt-sync')({sigint: true});




const hatChar = ' ^ ';

const fieldChar = '0';

const fallChar = ' O ';

class Field {
  constructor(field){

    this._field = field;
    
  }

}